import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // changed to prefer settings to allow for firebase to connect
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "MPA_assignment"
include ':app'
